spring:
  application:
    name: oauth-server-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/